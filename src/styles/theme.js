import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#d4ce41",
    },
    secondary: {
      main: "#fa8080",
      light: "#fbb4b4",
    },
  },
});
export default theme;
